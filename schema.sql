/* Database schema to keep the structure of entire database. */

CREATE TABLE ANIMALS (
    id INT,
    name VARCHAR,
    date_of_birth DATE,
    escape_attempts INT,
    neutered BOOLEAN,
    weight_kg DECIMAL  
);
